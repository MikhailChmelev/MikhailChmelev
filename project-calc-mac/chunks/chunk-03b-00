=.7?'ok':r.lambda<=.3?'bad':'warn')}${metric('Макс. ускорение Aω²',fmt(amax,5),'м/с²')}${metric('Скорость измеренная',fmt(vMeasured,6),'м/с')}${metric('Скорость теоретическая',fmt(vTheory,6),'м/с')}${metric('Производительность Q',fmt(Qmin,4),'дет/мин',ok?'ok':'bad')}${metric('Проверка загрузки',ok?'ОБЕСПЕЧИТ':'НЕ ОБЕСПЕЧИТ',`требуется ${fmt(target,2)} дет/мин`,ok?'ok':'bad')}</div><div class="table-wrap"><table><thead><tr><th>Параметр</th><th>Значение</th></tr></thead><tbody><tr><td>S′кр — вправо вверх</td><td>${fmt(S1,6)} м/с²</td></tr><tr><td>S″кр — влево вниз</td><td>${fmt(S2,6)} м/с²</td></tr><tr><td>Sот — отрыв</td><td>${fmt(Sot,6)} м/с²</td></tr><tr><td>ω</td><td>${fmt(r.w,6)} рад/с</td></tr><tr><td>Фаза ωt₀</td><td>${fmt(deg(phase),4)}°</td></tr></tbody></table></div>`;const text=`Лабораторная №13 — вибробункер\nγ=${deg(r.gamma)} град; A=${r.A} м; ω=${r.w} рад/с\nλ=${r.lambda}; режим: ${regime}\nS'кр=${S1}; S''кр=${S2}; Sот=${Sot} м/с²\nVизм=${vMeasured}; Vтеор=${vTheory} м/с\nQ=${Qmin} дет/мин; требуемая=${target}; результат=${ok?'обеспечит':'не обеспечит'}`;resultCard('Параметры вибробункера рассчитаны',regime,body,text)}

function renderAbout(){const html=`<section class="card"><div class="result-head"><div><h3>Архитектура программы</h3><p class="sub">Один автономный HTML-файл, без библиотек, сервера и подключения к интернету.</p></div><span class="status exact">ГОТОВО К РАБОТЕ</span></div><div class="metric-grid">${metric('Точные расчётные модули','№1, №2, №3, №4, №6, №13','по формулам методички')}${metric('Настраиваемые модели','№5 и №9','дискретные события / циклограмма')}${metric('Таблицы вариантов','1–5','встроены в файл')}${metric('Экспорт','Печать / PDF','и копирование результата')}</div><h3>Важные допущения</h3><div class="note warning">Лабораторная №5 в исходном документе требует конкретную функциональную схему РТК, задаваемую преподавателем, и старую систему PROJECT. Поэтому модуль №5 реализован как универсальный дискретно-событийный планировщик. Он рассчитывает те же целевые показатели — производительность и коэффициенты загрузки — но структуру операций необходимо настроить под выданную схему.</div><ul><li>№1 дополнительно учитывает внутреннюю границу достижимости двухзвенника |C₁−C₂|.</li><li>№2 решается точно как линейная задача по всем вершинам допустимой области.</li><li>№3 использует сеточную оптимизацию a, b; точность результата зависит от выбранного шага.</li><li>№4 имеет стандартный режим по методичке и расширенный worst-case перебор знаков скоростей/ускорений.</li><li>№13 теоретическая скорость считается по упрощённой формуле, прямо разрешённой в документе; производительность можно считать по измеренной скорости.</li></ul><h3>Как запускать</h3><ol><li>Сохраните файл на компьютер.</li><li>Откройте двойным кликом в Chrome, Яндекс Браузере, Edge или Safari.</li><li>Выберите лабораторную слева, загрузите вариант и запустите расчёт.</li></ol><div class="note">Все данные обрабатываются локально в браузере. Никаких серверов