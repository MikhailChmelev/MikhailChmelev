h.cos(r.alpha)/Math.sin(r.gamma-r.alpha),amax=r.A*r.w*r.w;let regime=r.lambda>=1?'Скольжение по лотку':r.lambda>.3?'Скольжение с подбрасыванием':'Скачкообразное движение (нерабочий режим)';if(r.lambda>=.35&&r.lambda<=.7)regime+=' · рабочий диапазон';const vMeasured=time>0?path/time:NaN,vTheory=r.A*r.w*Math.cos(phase)*Math.cos(r.gamma-r.alpha),v=$('l13speedSource').value==='1'?vMeasured:vTheory,Qsec=Kz*Kor*v/L,Qmin=Qsec*60,ok=Qmin>=target;const body=`<div class="metric-grid">${metric('Угол γ',fmt(deg(r.gamma),5),'град')}${metric('Амплитуда A',fmt(r.A*1000,6),'мм')}${metric('Критерий λ',fmt(r.lambda,6),regime,r.lambda>=.35&&r.lambda<